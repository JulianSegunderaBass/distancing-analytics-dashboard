const testDate = new Date();
const testDate2 = Date();

console.log(`${testDate} / is of type ${typeof testDate}`);
console.log(`${testDate2} / is of type ${typeof testDate2}`);

console.dir(testDate);